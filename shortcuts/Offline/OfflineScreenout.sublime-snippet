<snippet>
  <content><![CDATA[
<validate>
scrErr(${1})
</validate>

<exec>
status(${1}, "${2}")
</exec>

<term label="${2}_Term" cond="${1}">${2} term</term>

<suspend/>

${4:<row label="${2}" value="${3}">${2} ScreenOut</row>}
]]></content>
  <!-- Optional: Set a tabTrigger to define how to trigger the snippet -->
  <tabTrigger>scr.off</tabTrigger>
  <!-- Optional: Set a scope to limit where the snippet will trigger -->
  <scope>text.xml</scope>
</snippet>
